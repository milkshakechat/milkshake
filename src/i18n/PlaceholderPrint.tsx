/**
 * <pp>Placeholder Print</pp>
 *
 * Use <pp> during development to quickly add English text (default)
 * Later replace with i18n text. Use text search for "<pp>"
 * Allows for rapid development.
 */
const PP = ({ children }: { children: React.ReactNode }) => {
  return <> {children} </>;
};

export default PP;
