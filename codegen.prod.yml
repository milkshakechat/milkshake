overwrite: true
schema: "https://milkshake-sockets-hcdyzvq35a-wm.a.run.app/graphql"
documents: "src/**/*.tsx"
generates:
  src/api/graphql/types.ts:
    presets:
      - "client"
    plugins:
      - "typescript"
      - "typescript-operations"
